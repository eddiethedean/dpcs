dpcsVersion: "1.0.0"
id: "invalid.empty.retry"
version: "0.1.0"
interface:
  inputs: []
  outputs: []
steps:
  - id: "step_a"
    type: "extension:noop"
graph:
  edges: []
failureSemantics:
  - id: "empty_retry"
    scope:
      kind: "pipeline"
    triggers:
      - "transient"
    responses:
      - "retry"
    retry: {}
