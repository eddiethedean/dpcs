dpcsVersion: "1.0.0"
id: "invalid.failure.semantics"
version: "0.1.0"
interface:
  inputs: []
  outputs: []
steps:
  - id: "step_a"
    type: "extension:noop"
graph:
  edges: []
failureSemantics:
  - id: "broken_failure"
    scope:
      kind: "step"
      stepId: "missing_step"
    triggers: []
    responses:
      - "retry"
